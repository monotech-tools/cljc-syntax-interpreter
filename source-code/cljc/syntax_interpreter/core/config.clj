
(ns syntax-interpreter.core.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @constant (map)
(def DEFAULT-TAG-OPTIONS {:pattern-limits {:lookahead 0 :lookbehind 0 :match 64}})
